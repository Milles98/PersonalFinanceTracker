<UserControl x:Class="PersonalFinanceTracker.Views.TransactionEntryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:PersonalFinanceTracker.Views"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Top" Width="300">
            <TextBlock Text="Description" Margin="10 10"></TextBlock>
            <TextBox x:Name="DescriptionTextBox" Margin="10 10" Text="{Binding NewTransaction.Description, UpdateSourceTrigger=PropertyChanged}"></TextBox>
            <TextBlock Text="Amount" Margin="10 10"></TextBlock>
            <TextBox x:Name="AmountTextBox" Margin="10 10" Text="{Binding NewTransaction.Amount, UpdateSourceTrigger=PropertyChanged}"></TextBox>
            <TextBlock Text="Category" Margin="10 10"></TextBlock>
            <ComboBox x:Name="CategoryComboBox" Margin="10 10" ItemsSource="{Binding Categories}" SelectedItem="{Binding NewTransactions.Category}"></ComboBox>
            <Button Content="Add Transaction" Width="150" Margin="0 20 0 0" Command="{Binding AddTransactionCommand}"></Button>
        </StackPanel>
    </Grid>
</UserControl>
